# Copy to .env.local

# Backend URL (use /api when running dev server with Vite proxy, or full URL for production)
VITE_API_URL=/api

# For production, set the full backend URL:
# VITE_API_URL=http://192.168.1.10:8000
